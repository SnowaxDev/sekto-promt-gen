replicate>=0.34.0
anthropic>=0.40.0
fastapi>=0.115.0
uvicorn>=0.30.0
pydantic>=2.0.0
python-dotenv>=1.0.0   # loads .env automatically (a built-in fallback exists too)
pymongo>=4.6.0   # optional — only needed if MONGO_URI is set
