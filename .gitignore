# secrets / local env
.env

# runtime state (live copies the loop mutates — keep the seed, not these)
data/patterns.json
data/store.json
data/tasks.json
data/blocks.json

# locally saved generations (images + sidecars live on the user's PC only)
outputs/

# python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
